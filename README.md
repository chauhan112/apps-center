# setup

1. copy the repo and run clone the repo
```git submodule add git@github.com:chauhan112/JSlib.git src/rlib```

```git submodule update --init --recursive```

2. change the repo name in pyproject.toml